node_modules/
dist/
web-ext-artifacts/
*.zip
.env.local
.env.*.local
*.local
.DS_Store
test-results/
playwright-report/

# API keys for live-tier e2e (never commit)
.env.production

# AMO signing credentials (never commit)
credentials/

# Unzipped copy of the committed .json.gz capture fixture (scripts/tests
# gunzip on demand); raw captures must go through scripts/scrub-capture.ts
src/services/youtube/fixtures/gotham-poor-ranking-capture.json
src/services/youtube/fixtures/pii-warning-*

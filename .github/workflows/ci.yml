name: CI

on:
  push:
    branches: ["main"]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Type check
        run: npm run check
      - name: Unit tests
        run: npm test
      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium
      - name: E2E tests (demo mode, no network/keys)
        run: npm run test:e2e
      - name: Build & package
        run: npm run zip
      - name: Upload extension artifact
        uses: actions/upload-artifact@v4
        with:
          name: winnow-extension
          path: web-ext-artifacts/*.zip
      - name: Sign for AMO (unlisted)
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          MOZILLA_ADDONS_JWT_ISSUER: ${{ secrets.MOZILLA_ADDONS_JWT_ISSUER }}
          MOZILLA_ADDONS_JWT_SECRET: ${{ secrets.MOZILLA_ADDONS_JWT_SECRET }}
        run: node scripts/sign.mjs
      - name: Attach zip and signed xpi to release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" --generate-notes web-ext-artifacts/*.zip web-ext-artifacts/*.xpi \
            || gh release upload "${GITHUB_REF_NAME}" web-ext-artifacts/*.zip web-ext-artifacts/*.xpi
